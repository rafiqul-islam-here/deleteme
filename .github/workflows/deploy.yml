# name: Build and Push to GHCR

# on:
#   push:
#     branches:
#       - master

# permissions:
#   contents: read
#   packages: write

# jobs:
#   build-and-push:
#     runs-on: ubuntu-latest
#     steps:
#       - name: Checkout Repository
#         uses: actions/checkout@v4

#       - name: Set up Docker Buildx
#         uses: docker/setup-buildx-action@v3

#       - name: Log in to GitHub Container Registry
#         uses: docker/login-action@v3
#         with:
#           registry: ghcr.io
#           username: ${{ github.actor }}
#           password: ${{ secrets.GITHUB_TOKEN }}

#       - name: Downcase Repository Owner
#         run: |
#           echo "OWNER_LC=${OWNER,,}" >> ${GITHUB_ENV}
#         env:
#           OWNER: '${{ github.repository_owner }}'

#       - name: Build and Push Nginx Image
#         uses: docker/build-push-action@v5
#         with:
#           context: ./nginx
#           push: true
#           tags: |
#             ghcr.io/${{ env.OWNER_LC }}/deleteme-nginx:latest
#             ghcr.io/${{ env.OWNER_LC }}/deleteme-nginx:${{ github.sha }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Build and Push Frontend Image
#         uses: docker/build-push-action@v5
#         with:
#           context: ./frontend
#           push: true
#           tags: |
#             ghcr.io/${{ env.OWNER_LC }}/deleteme-frontend:latest
#             ghcr.io/${{ env.OWNER_LC }}/deleteme-frontend:${{ github.sha }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max

#       - name: Build and Push Backend Image
#         uses: docker/build-push-action@v5
#         with:
#           context: ./backend
#           push: true
#           tags: |
#             ghcr.io/${{ env.OWNER_LC }}/deleteme-backend:latest
#             ghcr.io/${{ env.OWNER_LC }}/deleteme-backend:${{ github.sha }}
#           cache-from: type=gha
#           cache-to: type=gha,mode=max
